# V2 implementation note

The v2 factory is the active model-generation path. The original v1 configuration and scripts remain in the repository only for checkpoint-format compatibility and regression tests. New production runs should use `gpt_bias_fusion_factory_v2.yaml` and `run_bias_fusion_factory_v2.sh`.
